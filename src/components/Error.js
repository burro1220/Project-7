import React from 'react';

//render error NOT FOUND page

const Error = () => (
    <div>
        <h1>Page Not Found</h1>
        <p>We're sorry but that page doesn't exist.</p>
    </div>
);

export default Error;